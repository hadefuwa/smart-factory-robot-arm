cd C:\Users\HamedA\Documents\sf2
git status
git add .
git commit -m "robot arm connected bit"
git push origin main
ssh pi@rpi "cd ~/sf2 && git pull"
ssh pi@rpi "cd ~/sf2 && git pull && sudo systemctl restart smart-factory.service"
